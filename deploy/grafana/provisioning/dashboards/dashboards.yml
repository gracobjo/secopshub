apiVersion: 1

providers:
  - name: secops
    orgId: 1
    folder: SecOps
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
